# aipocket T7 增量整改与独立验收报告

日期：2026-08-03（UTC+08:00）
结论：**PASS**

## 范围

T7 在保留 T6 共享工作区和现有 Compose 数据的前提下，处理以下项目：探测链路 SSRF 防护、前端包管理器完整性、登录失败体验、Compose 非破坏性恢复，以及审计记录生命周期判断。

## 已交付

### SSRF 防护

- 新增集中式目标策略，限制为公共 `http`/`https` 默认端口，拒绝 URL 凭据、无主机地址及受限 IPv4/IPv6 类别。
- DNS 每次解析均检查完整地址集合；空集合、私有地址及公私混合集合全部 fail closed。
- 每次重定向均重新校验目标，并执行重定向次数限制。
- 探测客户端禁用代理，超时配置非法时拒绝初始化，不回退到普通客户端。
- scanner 在创建任务前预检目标，并保留 `TargetProbe` 网络能力授权。

独立聚焦测试：`cargo test -p aipocket-services ssrf -- --nocapture`，6/6 通过。

### 前端可复现性与登录体验

- `frontend/package.json` 的 pnpm 10.25.0 完整性值已纠正；冷缓存 Corepack 可解析精确版本。
- 登录 401 映射为本地化且不泄露后端细节的提示，其他失败使用通用提示。
- 错误提示具备 alert/ARIA 关联及聚焦行为，编辑后清除；密码显示按钮保持 44x44 像素点击区域。
- 新增回归测试覆盖错误映射、焦点、ARIA、清除行为和密码按钮尺寸。

独立前端门禁：pnpm 10.25.0；lint 0 errors（14 个既有 warnings）；6 个测试文件、42 个测试通过；生产构建通过（1970 modules）。

### Compose 恢复

- 在项目 `aipocket-t6-deploy` 上非破坏性重建并重建仅应用容器；PostgreSQL 与 Redis 未删除、未重建卷。
- 最终 backend、frontend、PostgreSQL、Redis 四服务全部 healthy。
- `http://127.0.0.1:18006/api/health` 返回 HTTP 200 和 `{"ok":true}`。
- `http://127.0.0.1:13086/` 返回 HTTP 200，页面标题为 `aipocket`。
- 三个命名卷在恢复前后保持：`postgres-data`、`redis-data`、`results`（完整名称均带项目名前缀）。

### 审计生命周期

`request_ledger` 是 run 生命周期内追加的请求尝试分母。当前 schema 明确使用级联删除，历史界面也明确提示删除 run 会删除相关记录；现有需求或 ADR 没有承诺 WORM、法务或合规留存。因此本轮不把级联删除判定为缺陷，也未改动该契约。若未来要求持久合规审计，应先定义留存期、删除权限、导出/脱敏和独立存储边界。

## 门禁结果

- `cargo fmt --all -- --check`：PASS。
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`：PASS。
- `cargo test --workspace --all-targets --all-features`：PASS；独立日志记录 6 个集成测试通过、2 个 ignored，其他 crate 单测/文档测试通过。
- SSRF 聚焦测试：6/6 PASS。
- pnpm lint/test/build：PASS。
- Docker 镜像构建与运行健康检查：PASS。

原始独立日志位于 `/home/hehecat/code/apps/generic-agent/temp/aipocket-goal-t7/workers/verifier/logs/`，独立结论见同目录上级 `report.md`。

## 已知边界

- 共享工作区同时包含 T7 之前和多席位改动，本报告不把整个 dirty diff 归因于 T7。
- Compose 验证覆盖同一主机和 daemon 上的定向重建、依赖重启与服务恢复，不等同于 daemon 重启或主机断电测试。
- 已验证卷身份与服务可访问性；为避免触碰数据，未检查 PostgreSQL、Redis 和结果卷的内容，因此不声称内容级持久性。
- 安全测试使用本地确定性地址和解析器，无外部 provider 调用或真实攻击。
- lint 的 14 个 warning 为非阻断既有告警；本轮验收标准为 0 error。
