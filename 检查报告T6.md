# AIPocket T6 全量复验报告

日期：2026-08-03（UTC+08:00）  
仓库：`/home/hehecat/code/apps/aipocket`  
基线：`de79f7d3115aead54bdb859d292fb4a8cf74382a`  
范围：只读检查、构建、隔离部署和浏览器验证；未修改产品源码、测试、锁文件或已有共享工作树改动。

## 结论

**有条件通过，不应视为无风险上线放行。**

- P0：0 项。
- P1：2 项。启用出站扫描后的条件性认证 SSRF；原始前端 `pnpm` 验收命令因 Corepack 包哈希不一致无法执行。
- P2：5 项。扫描删除同步删除请求账本；Compose 并行重启时后端发生一次依赖 DNS 瞬态退出；登录错误提示未本地化；校验失败后的焦点与无障碍错误语义不足；密码可见性按钮命中区仅 `15x15` CSS 像素。

Rust 门禁、前端底层工具、镜像构建、隔离 Compose 运行、重启恢复和登录前浏览器流程均取得通过证据。认证后业务旅程、真实 provider、数据库内容级持久化、被忽略的在线集成测试及当前 Rust 覆盖率未验证。

## P1

### P1-1 启用出站扫描后存在条件性认证 SSRF

**条件：** 管理员显式启用出站扫描，攻击者已取得控制台认证权限。

手工目标保存仅做 URL/源站语法处理，未拒绝 loopback、链路本地、RFC1918、ULA、云元数据地址，也未对 DNS 解析、重绑定和每次重定向后的目的地址实施网络边界校验。该目标随后可进入被动未认证探测；HTTP 客户端虽限制为两次重定向，但未限制每跳目的地。

默认关闭出站发现与主动探测显著降低默认暴露，但不能消除功能启用后的风险。修复应在连接前及每次重定向后统一校验解析地址，并用仅指向本地 mock listener 的自动化回归测试覆盖 loopback、私网、链路本地、元数据地址、DNS 重绑定和重定向绕过。

静态证据集中于：

- `crates/aipocket-api/src/routes.rs`：手工目标保存与来源加载。
- `crates/aipocket-services/src/scanner.rs`：目标进入探测流程。
- `crates/aipocket-prober/src/products.rs`、`engines.rs`：拼接目标并发起 HTTP GET。
- `crates/aipocket-api/src/state.rs`：重定向有次数上限但无逐跳目的地址策略。

### P1-2 原始前端验收入口不可复现

以下原始命令均在脚本执行前退出 1：

- `pnpm lint`
- `pnpm test -- --run`
- `pnpm build`

Corepack 尝试下载固定的 `pnpm@10.25.0` 时报告 tarball hash mismatch。禁用完整性密钥和切换 npm registry 均未改变结果。因此这是包管理器引导与可复现性故障，不是前端测试失败。

已检出的同项目 `node_modules/.bin` 后备工具全部通过：oxlint 0 error/14 warning、Vitest 5 文件 38 测试通过、TypeScript 无诊断、Vite 生产构建成功。仍需修正 Corepack/镜像配置，使仓库声明的标准命令能在干净环境直接执行。

## P2

### P2-1 请求账本随扫描删除

迁移中的外键将 `request_ledger.scan_id` 配置为 `ON DELETE CASCADE`，删除扫描会同时删除请求账本。若该表仅为运行遥测，这是可接受的生命周期语义；若其承担持久安全审计职责，则不满足保留预期。应明确定位，并相应采用保留、归档或不可变审计存储。

### P2-2 并行重启存在一次瞬态依赖解析失败

`docker compose restart` 最终在约 `8.105 s` 后恢复 4/4 healthy，容器 ID 与三个命名卷身份均保持不变，API 与前端继续返回 200。但并行重启期间 backend 曾因 `postgres` 主机名暂不可解析而退出一次，随后由 `restart: unless-stopped` 自动恢复，最终 `restart_count=1`。建议增加启动期依赖等待与带退避的数据库连接重试。

### P2-3 登录错误提示未本地化

错误密码正确返回 HTTP 401 且页面保持可用，但中文界面直接显示英文 `invalid password`。建议映射为一致、可操作的中文提示。

### P2-4 校验失败后的焦点与错误语义不足

空提交或错误密码提交后仅显示 toast，焦点落到 `BODY`；密码输入框没有 `aria-invalid`，也没有关联的错误说明。建议将焦点恢复到密码框，并通过 `aria-invalid`、`aria-describedby` 或等效 live region 暴露错误。

### P2-5 密码可见性按钮命中区过小

桌面与移动端实测按钮均为 `15x15` CSS 像素，低于常见的 `24x24` 最低目标和 `44x44` 触控建议。图标可保持原尺寸，但应扩大按钮可点击区域。

## 实测结果

### Rust 与前端

| 检查 | 结果 | 说明 |
|---|---:|---|
| `cargo fmt --all -- --check` | PASS | rc 0，约 1 s |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS | rc 0，约 46 s |
| `cargo test --workspace --all-targets --all-features` | PASS | 130 passed / 0 failed / 10 ignored |
| 前端标准 `pnpm` 三命令 | BLOCKED | Corepack tarball hash mismatch，均 rc 1 |
| `node_modules/.bin/oxlint src` | PASS | 0 error / 14 warning |
| `node_modules/.bin/vitest run` | PASS | 38/38 tests |
| `node_modules/.bin/tsc -b` | PASS | 无诊断 |
| `node_modules/.bin/vite build` | PASS | 1,970 modules，生产构建成功 |
| `git diff --check` | PASS | rc 0 |

安全相关聚焦测试另有 54 项通过：API 9、clients 9、core 15、discovery 12、prober 18；scheduler 过滤测试 5/5 通过。密钥默认关闭、默认拒绝开关、错误脱敏、有限重试、分页上限、去重和调度恢复路径均获得测试或源码证据。

### 本地隔离部署

- `docker compose config --quiet` 通过，两套应用镜像构建成功。
- 首次 `up -d --build --wait` 因宿主 Docker predefined address pools 耗尽而失败，不能记为本机开箱首次成功。
- 为该 Compose 项目预建带标签、无路由冲突的 `10.254.246.0/24` bridge 后，`up -d --wait` 在约 `19.320 s` 完成。
- 最终 backend、frontend、PostgreSQL、Redis 均 healthy。
- 前端 `http://127.0.0.1:13086/`、API `http://127.0.0.1:18006/` 及前端引用的 4 个静态资源均返回 HTTP 200。
- 宿主仅回环发布 frontend/backend；PostgreSQL 和 Redis 未发布宿主端口。
- 非破坏性重启后容器和卷身份不变，但未插入哨兵数据，因此不宣称数据库行或 Redis key 的内容级持久化已验证。

隔离栈在检查结束时保持运行，供本地复核使用。

### 浏览器旅程

Google Chrome 145 headless CDP 在桌面 `1440x1000` 和移动 `390x844` 实测：

- 登录页完整渲染，静态资源均为 200，无运行时异常。
- 未认证访问 `/dashboard` 与 `/history` 均被路由守卫重定向到 `/login`。
- 密码可见性切换有效，标签在“显示密码/隐藏密码”之间更新。
- 空提交不发起认证请求并显示必填提示。
- 合成错误密码仅向回环 API 发起一次请求，返回预期 401。
- 移动端 `scrollWidth == innerWidth == 390`，未测得横向溢出或主控件重叠。

Chrome 另提示密码表单宜包含用户名字段；本次作为浏览器建议记录，未单独升级缺陷。

## 证据边界

**已实测：** Rust 全工作区门禁、前端底层 lint/test/typecheck/build、聚焦安全测试、Compose 配置与镜像构建、隔离部署健康、HTTP 资源、非破坏性重启、桌面/移动登录前流程。

**静态审查：** SSRF 可达链、重定向目的地址策略缺失、请求账本级联删除、默认关闭与脱敏/重试/分页/调度路径。

**未验证：** 真实 provider 行为与限流、认证成功后的 dashboard/history/扫描流程、真实扫描、扫描错误/空态/重试体验、10 个 ignored 在线集成测试、当前 Rust 数值覆盖率、数据库内容级持久化、Docker 镜像在其他宿主环境的首次启动、反向代理/防火墙/数据库授权与合规级审计不可变性。

## 发布建议

1. 在任何可访问可信内网或云元数据服务的环境启用出站扫描前，先修复 P1-1。
2. 恢复标准 `pnpm` 门禁的干净环境可复现性，并将原始命令重新跑通。
3. 明确 `request_ledger` 的审计保留合同，再决定是否移除级联删除。
4. 补强 backend 启动依赖恢复和三项登录体验问题。
5. 使用授权的测试凭据与隔离 provider/mock 环境补做认证后 E2E、在线集成测试和覆盖率门禁。

## 原始证据

完整逐命令日志、截图、CDP 状态和各席报告保存在：

`/home/hehecat/code/apps/generic-agent/temp/aipocket-goal-t6/workers/`
